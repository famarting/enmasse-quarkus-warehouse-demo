<configuration xmlns="urn:activemq" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="urn:activemq /schema/artemis-configuration.xsd">
    <core xmlns="urn:activemq:core">
        <paging-directory>./target/artemis/paging</paging-directory>
        <bindings-directory>./target/artemis/bindings</bindings-directory>
        <journal-directory>./target/artemis/journal</journal-directory>
        <large-messages-directory>./target/artemis/large-messages</large-messages-directory>

        <!-- <connectors> -->
            <!-- <connector name="activemq">tcp://localhost:5672</connector> -->
        <!-- </connectors> -->
        <acceptors>
            <!-- <acceptor name="activemq">tcp://localhost:5672</acceptor> -->
            <acceptor name="amqp">tcp://0.0.0.0:5672?protocols=AMQP;saslMechanisms=NONE</acceptor>
        </acceptors>

        <security-enabled>false</security-enabled>

        <!-- <queues>
            <queue name="foo">
                <address>test-jms</address>
            </queue>
        </queues> -->

        <address-settings>
            <!-- if you define auto-create on certain queues, management has to be auto-create -->
            <address-setting match="activemq.management#">
               <dead-letter-address>DLQ</dead-letter-address>
               <expiry-address>ExpiryQueue</expiry-address>
               <redelivery-delay>0</redelivery-delay>
               <!-- with -1 only the global-max-size is in use for limiting -->
               <max-size-bytes>-1</max-size-bytes>
               <message-counter-history-day-limit>10</message-counter-history-day-limit>
               <address-full-policy>PAGE</address-full-policy>
               <auto-create-queues>true</auto-create-queues>
               <auto-create-addresses>true</auto-create-addresses>
               <auto-create-jms-queues>true</auto-create-jms-queues>
               <auto-create-jms-topics>true</auto-create-jms-topics>
            </address-setting>
            <!--default for catch all-->
            <address-setting match="#">
               <redelivery-delay>0</redelivery-delay>
               <!-- with -1 only the global-max-size is in use for limiting -->
               <max-size-bytes>15240</max-size-bytes>
               <message-counter-history-day-limit>10</message-counter-history-day-limit>
               <address-full-policy>FAIL</address-full-policy>
               <auto-create-queues>true</auto-create-queues>
               <auto-create-addresses>true</auto-create-addresses>
               <auto-create-jms-queues>true</auto-create-jms-queues>
               <auto-create-jms-topics>true</auto-create-jms-topics>
            </address-setting>
         </address-settings>
    </core>
</configuration>